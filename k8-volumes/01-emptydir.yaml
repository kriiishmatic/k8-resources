apiVersion : v1
kind : Pod
metadata :
  name : emptydir-pod
spec :
  containers :
  - name : emptydir-container
    image : nginx:latest
    volumeMounts :
    - name : emptydir-volume
      mountPath : /
  volumes :
  - name : emptydir-volume
    emptyDir : {}
    sizeLimit : 1Gi